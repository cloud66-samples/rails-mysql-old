rails:
  servers:
    server:
      unique_name: app
      size: t3.large
      region: ap-south-1
      vendor: aws
      cloud_key: Images
mysql:
  servers:
    server:
      unique_name: db
      size: t3.large
      region: ap-south-1
      vendor: aws
      cloud_key: Images
redis:
  servers:
    server:
      unique_name: redis
      size: t3.large
      region: ap-south-1
      cloud_key: Images
